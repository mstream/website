[Unit]
Description=JetBrains TeamCity
Requires=network.target
After=syslog.target network.target

[Service]
Type=forking
EnvironmentFile=/etc/sysconfig/teamcity
ExecStart={{ user_home }}/default/bin/teamcity-server.sh start
ExecStop={{ user_home }}/default/bin/teamcity-server.sh stop
User={{ user_name }}

[Install]
WantedBy=multi-user.target
